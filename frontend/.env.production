# Baked into `vite build` for live site — public origins only (no secrets).
# Same origin when apex points at cloneai-api (repo-root Dockerfile). Override for split API/static deploys.
VITE_API_URL=https://siteclonerpro.com
VITE_PUBLIC_APP_URL=https://siteclonerpro.com
