# Public URLs only (no secrets). Matches Render cloneai-web / monolith SPA build.
# Dev still uses Vite proxy (no VITE_API_URL); production build targets the live API hostname.
VITE_API_URL=https://siteclonerpro.com
VITE_PUBLIC_APP_URL=https://siteclonerpro.com
VITE_SAME_ORIGIN_API=true
