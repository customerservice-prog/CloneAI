# Render Blueprint — step-by-step: docs/RENDER_EASY.md
# Set secrets in the Render dashboard (never commit real keys).
# https://render.com/docs/blueprint-spec
#
# First deploy: set OPENAI_API_KEY in dashboard. CORS + FRONTEND_URL default to www.siteclonerpro.com.
# Billing later: BILLING_ENABLED=true + Stripe vars (see backend/.env.example).
#
# Stripe webhook (when billing on): https://<this-host>/api/billing/webhook
# Events: checkout.session.completed, customer.subscription.updated, customer.subscription.deleted, invoice.payment_failed
services:
  - type: web
    name: cloneai-api
    runtime: docker
    dockerfilePath: ./backend/Dockerfile
    dockerContext: ./backend
    plan: starter
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: "3001"
      # Easier first launch — turn on Stripe vars in dashboard when ready.
      - key: BILLING_ENABLED
        value: "false"
      - key: CORS_ORIGINS
        value: "https://www.siteclonerpro.com,https://siteclonerpro.com"
      - key: FRONTEND_URL
        value: "https://www.siteclonerpro.com"
      - key: OPENAI_API_KEY
        sync: false
